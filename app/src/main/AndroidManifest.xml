<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.joiner">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/jobr_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/jobr_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".WebViewActivity"></activity>
        <activity android:name=".WebViewActivty" />
        <activity android:name=".LoginPageActivity" />
        <activity android:name=".SelectorEmpleadosActivity" />
        <activity android:name=".TodoActivity" />
        <activity android:name=".RRHHActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>